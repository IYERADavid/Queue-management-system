/**
 * Design-pattern entry points used by the simulated persistence layer.
 * See `DESIGN_PATTERNS.md` at the repo root for how each maps to the course marking guide.
 */
export { InMemoryQueueStore } from './in-memory-queue-store'
export { TicketFactory } from './ticket-factory'
export { applyTicketStatusStrategy } from './ticket-status-strategies'
